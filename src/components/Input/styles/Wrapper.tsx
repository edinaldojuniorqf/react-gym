import styled from 'styled-components'

export const Wrapper = styled.div`
	display: flex;
	margin: 0 0 15px;

	input {
		flex: 1;
		display: flex;
		flex-direction: row;
		align-items: center;
		padding: 16px;
		gap: 8px;
		background: #121214;
		border-radius: 6px;
		border: none;
		outline: none;
		color: #7C7C8A;
		font-size: 1rem;
	}
`